// import { student } from '../Student/student'

export const conversationName = state => {
    return state.name;
}

export const oppositeConversationStudent = state => {
    // return state.students.filter(
        // s => s.id != student.id
    // );
}

export const conversationMessages = state => {
    return state.messages;
}
